# site

Astro本番ルートとは別の、静的LPや試作用ファイル置き場です。

## 現在の内容

| フォルダ | 内容 | 扱い |
|---|---|---|
| `money-v2/` | 借金/返済系の試作LP | 試作。公開本番は `src/pages/money/` を優先 |

## 注意

- `site/` 配下はそのままAstroの公開ルートになるとは限りません。
- 本番に反映する場合は `src/pages/` または `public/` への移植が必要です。
